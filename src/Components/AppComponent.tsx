import * as React from 'react';
import { MyCanvas } from './MyCanvas';

export const AppComponent = () => (
	<div className="mandala-app">
		<MyCanvas width={800} height={800} />
		<div>asdf</div>
	</div>
);
